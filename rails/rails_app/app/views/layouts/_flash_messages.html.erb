<% if flash.present? %>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "panel flash #{key}") %>   #ist ein helper, der html code anpasst

      <% end %>
    <% end %>